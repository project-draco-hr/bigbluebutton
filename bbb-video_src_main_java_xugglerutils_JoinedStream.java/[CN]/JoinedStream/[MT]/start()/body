{
  startPublishing(scope);
  setupStreams();
  pictureSender=new Runnable(){
    public void run(){
      while (keepStreaming) {
        encodePicture(imageProcessor.getImage());
      }
    }
  }
;
}
