{
  while (keepCapturing) {
    try {
      acceptRoomConnection(serverSocket.accept());
    }
 catch (    IOException e) {
      System.out.println(e.getMessage());
      e.printStackTrace(System.out);
    }
  }
}
