{
  if (log.isDebugEnabled()) {
    log.debug("disconnect: {} << {}",conn,scope);
  }
  if (isApp(scope)) {
    appDisconnect(conn);
  }
 else   if (isRoom(scope)) {
    roomDisconnect(conn);
  }
  super.disconnect(conn,scope);
}
