{
  for (  String name : list) {
    try {
      Class<? extends BBBFrameRecorder> c=get(name);
      c.getMethod("tryLoad").invoke(null);
      return c;
    }
 catch (    Throwable t) {
    }
  }
  return null;
}
