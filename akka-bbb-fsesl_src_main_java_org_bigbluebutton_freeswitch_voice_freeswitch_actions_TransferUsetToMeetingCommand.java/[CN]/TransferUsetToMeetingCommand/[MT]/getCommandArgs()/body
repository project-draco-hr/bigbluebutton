{
  String action="";
  if (forward)   action="conference" + SPACE + room+ SPACE+ "transfer"+ SPACE+ targetRoom;
 else {
    action="conference" + SPACE + targetRoom+ SPACE+ "transfer"+ SPACE+ room;
  }
  return action + SPACE + participant;
}
