{
  processBlocks=true;
  while (processBlocks) {
    Message message;
    try {
      message=retriever.getNextMessageToSend();
      processNextMessageToSend(message);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
  try {
    outstream.close();
    outstream=null;
    socket.close();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
