{
  log.debug("closeMediaApplication","Init...");
  if (audioApp != null) {
    audioApp.stopMedia();
    audioApp=null;
  }
  if (videoApp != null) {
    videoApp.stopMedia();
    videoApp=null;
  }
}
