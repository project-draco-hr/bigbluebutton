{
  if (pos == Long.MAX_VALUE) {
    in.position(in.limit());
    currentTime=duration;
    return;
  }
  in.position((int)pos);
  searchNextFrame();
  analyzeKeyFrames();
  Double time=posTimeMap.get(in.position());
  if (time != null) {
    currentTime=time;
  }
 else {
    currentTime=0;
  }
}
