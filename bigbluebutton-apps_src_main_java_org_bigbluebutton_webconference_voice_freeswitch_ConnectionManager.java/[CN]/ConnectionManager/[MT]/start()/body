{
  sendMessages=true;
  Runnable sender=new Runnable(){
    public void run(){
      while (sendMessages) {
        connect();
      }
    }
  }
;
  connExec.scheduleWithFixedDelay(sender,0,1000,TimeUnit.SECONDS);
}
