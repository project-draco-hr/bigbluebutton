{
  Jedis jedis=new Jedis("localhost");
  log.debug("subscribing to the channels...");
  try {
    jedis.connect();
  }
 catch (  UnknownHostException e) {
    e.printStackTrace();
  }
catch (  IOException e) {
    e.printStackTrace();
  }
  subscribe(jedis);
}
