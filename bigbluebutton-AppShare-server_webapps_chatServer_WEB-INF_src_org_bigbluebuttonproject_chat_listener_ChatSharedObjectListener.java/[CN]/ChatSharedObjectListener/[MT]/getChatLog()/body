{
  StringBuffer log=new StringBuffer();
  Object[] obj=buffer.toArray();
  if (!buffer.isEmpty()) {
    for (int i=0; i < buffer.size(); i++) {
      log.append(obj[i] + "");
    }
  }
  return log.toString();
}
