{
  final int len=encoded.length;
  int pos=stringReferences.indexOf(str);
  if (pos >= 0) {
    putInteger(pos << 1);
    return;
  }
  putInteger(len << 1 | 1);
  buf.put(encoded);
  stringReferences.add(str);
}
