{
  this.host=host;
  this.room=room;
  this.videoWidth=videoWidth;
  this.videoHeight=videoHeight;
  this.frameRate=frameRate;
  if (openConnection()) {
    try {
      sendCaptureStartEvent();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
}
