{
  if (in.prefixedDataAvailable(4,MAX_IMAGE_SIZE)) {
    BufferedImage image;
    try {
      image=readImage(in);
      session.setAttribute(CAPTURED_SCREEN,image);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    return true;
  }
  return false;
}
