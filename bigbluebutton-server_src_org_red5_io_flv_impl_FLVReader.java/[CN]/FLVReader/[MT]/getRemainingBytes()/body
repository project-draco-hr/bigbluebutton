{
  if (!useLoadBuf) {
    return in.remaining();
  }
  try {
    return channelSize - channel.position() + in.remaining();
  }
 catch (  Exception e) {
    log.error("Error getRemainingBytes",e);
    return 0;
  }
}
