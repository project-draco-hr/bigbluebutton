{
  if (o == null) {
    return false;
  }
  if (o instanceof Boolean) {
    return (Boolean)o;
  }
  final String s;
  if (o instanceof String) {
    s=(String)o;
  }
 else {
    s=o.toString();
  }
  return TRUE_LITERALS.contains(s.toLowerCase(Locale.US));
}
